apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    pvc: prometheus-data
  name: prometheus-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
